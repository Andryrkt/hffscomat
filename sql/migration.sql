CREATE TABLE bc_client_soumis_neg
   AS
   SELECT * FROM ir_scomat:regix.bc_client_soumis_neg;


CREATE TABLE bc_soumis
   AS
   SELECT * FROM ir_scomat:regix.bc_soumis;

CREATE table categorie_ate_app
   AS
   SELECT * FROM ir_scomat:regix.categorie_ate_app;

CREATE table ces_magasin
   AS
   SELECT * FROM ir_scomat:regix.ces_magasin;

CREATE table demande_intervention
   AS
   SELECT * FROM ir_scomat:regix.demande_intervention;

CREATE table devis_soumis_a_validation
   AS
   SELECT * FROM ir_scomat:regix.devis_soumis_a_validation;

CREATE table devis_soumis_a_validation_neg
   AS
   SELECT * FROM ir_scomat:regix.devis_soumis_a_validation_neg;

CREATE table dw_bc_client
   AS
   SELECT * FROM ir_scomat:regix.dw_bc_client;

CREATE table dw_bc_client_negoce
   AS
   SELECT * FROM ir_scomat:regix.dw_bc_client_negoce;

CREATE table dw_commande
   AS
   SELECT * FROM ir_scomat:regix.dw_commande;

CREATE table dw_demande_intervention
   AS
   SELECT * FROM ir_scomat:regix.dw_demande_intervention;

CREATE table dw_devis_ate
   AS
   SELECT * FROM ir_scomat:regix.dw_devis_ate;

CREATE table dw_facture
   AS
   SELECT * FROM ir_scomat:regix.dw_facture;

CREATE table dw_ordre_de_reparation
   AS
   SELECT * FROM ir_scomat:regix.dw_ordre_de_reparation;

CREATE table dw_rapport_intervention
   AS
   SELECT * FROM ir_scomat:regix.dw_rapport_intervention;

CREATE table facture_soumis_a_validation
   AS
   SELECT * FROM ir_scomat:regix.facture_soumis_a_validation;

CREATE table ors_soumis_a_validation
   AS
   SELECT * FROM ir_scomat:regix.ors_soumis_a_validation;

CREATE table pointage_relance
   AS
   SELECT * FROM ir_scomat:regix.pointage_relance;

CREATE table ri_soumis_a_validation
   AS
   SELECT * FROM ir_scomat:regix.ri_soumis_a_validation;

CREATE table statut_demande
   AS
   SELECT * FROM ir_scomat:regix.statut_demande;

CREATE table wor_niveau_urgence
   AS
   SELECT * FROM ir_scomat:regix.wor_niveau_urgence;

CREATE table wor_type_document
   AS
   SELECT * FROM ir_scomat:regix.wor_type_document;