# Configuration des services de gestion des fichiers
# Fichier : config/services/services_fichier.yaml

services:
    # Service d'upload de fichiers
    App\Service\fichier\UploderFileService:
        arguments:
            $cheminDeBase: '%env(BASE_PATH_FICHIER)%/magasin/devis/'
        public: true

    # Ajoutez d'autres services de fichiers ici
    # App\Service\fichier\FileManagerService:
    #     arguments:
    #         $basePath: '%env(BASE_PATH_FICHIER)%/'
    #     public: true

    # App\Service\fichier\FileValidatorService:
    #     arguments:
    #         $allowedExtensions: ['pdf', 'doc', 'docx']
    #     public: true
