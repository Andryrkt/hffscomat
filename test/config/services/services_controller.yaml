# Configuration des contrôleurs avec injection de dépendances
# Fichier : config/services/services_controller.yaml

services:
    # Contrôleur auto-wiré pour la vérification de prix des devis magasin
    App\Controller\magasin\devis\DevisMagasinVerificationPrixControllerAutoWired:
        arguments:
            $cheminBaseUpload: '%env(BASE_PATH_FICHIER)%/magasin/devis/'
        public: true

    # Ajoutez d'autres contrôleurs avec injection de dépendances ici
    # App\Controller\magasin\devis\DevisMagasinController:
    #     arguments:
    #         $cheminBaseUpload: '%env(BASE_PATH_FICHIER)%/magasin/devis/'
    #     public: true

    # App\Controller\dit\DevisDitController:
    #     arguments:
    #         $cheminBaseUpload: '%env(BASE_PATH_FICHIER)%/dit/devis/'
    #     public: true
