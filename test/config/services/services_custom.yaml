# Configuration des services personnalisés
# Fichier : config/services/services_custom.yaml

services:
    # Services d'historique et de notification
    App\Service\historiqueOperation\HistoriqueOperationDevisMagasinService:
        public: true

    # Services de validation
    App\Service\magasin\devis\DevisMagasinValidationVpService:
        public: true

    # Services de gestion des versions
    App\Service\autres\VersionService:
        public: true

    # Ajoutez d'autres services personnalisés ici
    # App\Service\notification\EmailService:
    #     arguments:
    #         $smtpHost: '%env(SMTP_HOST)%'
    #         $smtpPort: '%env(SMTP_PORT)%'
    #     public: true

    # App\Service\security\SecurityService:
    #     arguments:
    #         $encryptionKey: '%env(ENCRYPTION_KEY)%'
    #     public: true
